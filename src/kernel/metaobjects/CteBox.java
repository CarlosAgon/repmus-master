package kernel.metaobjects;

import javafx.geometry.Point2D;
import kernel.annotations.Omclass;
import kernel.frames.simples.CteBoxFrame;
import kernel.interfaces.I_SimpleFrame;

public class CteBox extends Box {

	public CteBox(String val, Point2D pos, String name) {
	 super();
	/* if (val.equals("true") || val.equals("false") ) {
	 
	 } else {
		 if ()
	 }
	 */
}

/*
@Override
public I_SimpleFrame makeFrame() {
	I_SimpleFrame frame = new CteBoxFrame (this, frameposition );
	return frame;
}
*/
}
